extends ../layout

block content
    form(action="/lead/" + lead.id + "/edit" method="POST")
        div(class="form-group")
            input(name="lead_email", type="email", placeholder="Enter your email address", value=lead.email, class="form-control")
            button(type="submit", class="btn btn-primary") Save